import React, { useState } from 'react'

const Topnav = () => {
    const [nav, setNav] = useState(false)
    const handleNav = () => {
        setNav(!nav)
    }
    
    return (
        <div>
        </div>
    )
}

export default Topnav